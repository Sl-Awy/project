# Leave empty to use the same origin the app is served from (recommended).
# In dev, Vite proxies /api and /uploads to the PHP backend (see vite.config.ts).
# Set to an absolute URL only if the API runs on a different origin, e.g.:
#   VITE_API_URL=https://api.example.com
VITE_API_URL=
